spring:
  application:
    name: "configserver"
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
         # search-locations: "classpath:/config"
         earch-locations: "file:///Users//eazybytes//Documents//config"
server:
  port: 8071